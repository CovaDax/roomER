<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :building %>
    <%= f.text_field :building %><br /><br />
  </div>
  <div class="field">
    <%= f.label :number %>
    <%= f.text_field :number %><br /><br />
  </div>
  <div class="field">
    <%= f.label :maxOccupancy %>
    <%= f.text_field :maxOccupancy %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
