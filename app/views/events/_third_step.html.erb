        <div class="field">
          <b>Preferred Space:</b> If you are unsure, describe the type of meeting space you need in the notes section below.<br/>
          <%= f.collection_select :room_id, Room.where('maxOccupancy >= ?', @event.numAttends),
                                  :id, :choiceName, {:prompt => 'select room'} %><br/><br/>
        </div>
        
        <div class="field">
        	<%= f.label :notes %>
        	<%= f.text_area :notes  %><br/><br/>
        </div>

		  
		  
		  
		  